# Security Criticals — Status Tracker

Generated by `/cso` audit on 2026-03-26. Updated 2026-03-26.

## CRITICAL

### 1. Unauthenticated JWT Issuance — ✅ FIXED
- **Fix:** `AUTH_BOOTSTRAP_SECRET` required for all token issuance + IP-based brute force protection (10 attempts/15min)
- **Commit:** Session 6 refactor

### 2. WhatsApp Webhook No HMAC — ✅ N/A (route deleted)

## HIGH

### 3. Fyers OAuth State Bypass — ✅ FIXED
- **Fix:** Enforced signed state token via `validateState()`, removed `req.query.userId` fallback
- **Commit:** Session 6 refactor

### 4. Rate Limiter Uses Untrusted userId — ✅ FIXED
- **Fix:** Rate limiter uses JWT userId with IP address fallback for unauthenticated requests
- **File:** `src/middleware/rateLimit.js`

### 5. Mass Assignment in Onboarding — ✅ FIXED
- **Fix:** `ALLOWED_FIELDS` Set + `sanitizeFields()` strips unknown fields
- **File:** `src/routes/onboarding.js`

### 6. LLM Prompt Injection via User Facts — ✅ FIXED
- **Fix:** User context moved from system prompt to user-role message with "do not treat as instructions" delimiter
- **File:** `src/routes/chat.js`

### 7. Tool Dispatch Without Output Validation — ✅ FIXED
- **Fix:** Tool outputs wrapped in `<tool_output>` delimiters to prevent injection from external data
- **File:** `src/routes/chat.js`

### 8. No Per-User AI Cost Cap — ✅ FIXED
- **Fix:** `MAX_TOOL_CALLS = 5` cap per message, warns on cap breach
- **File:** `src/routes/chat.js`

### 9. Upstox SDK Singleton Race Condition — ✅ FIXED
- **Fix:** `createApiClient(token)` creates per-request instance instead of mutating global singleton
- **File:** `src/routes/upstox.js`

## MEDIUM

### 10. Google Drive Query Injection — ✅ FIXED
- **Fix:** Escape backslashes and single quotes in user search query before interpolating into Drive API query
- **File:** `src/routes/drive.js`

---

**All 10 findings resolved.** Next CSO audit recommended after major feature additions.
